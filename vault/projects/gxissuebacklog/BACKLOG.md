# Galaxy Issue Backlog

GitHub state last refreshed: 2026-09-20.

Branch and PR state lives in `vault/reviews/mine/index.md`; this file maps issues to
the branches that address them. Every issue listed here — in any section — is already
triaged and must be excluded from the next "find new issues" sweep.

## Queued — triaged, no branch yet (`queued`)

- [#23521](https://github.com/galaxyproject/galaxy/issues/23521) — Description: A workflow conditionally executing one of three subworkflows fails with `Expected [] to be hashable` on both 26.0 and 26.1; root cause found and reproduced: `collect_input_dataset_collections` reduces a collection into a non-multiple `data` parameter, and the element count is all that separates this from #19538 (1) and #22401 (2+); branch `issue_23521_empty_collection_single_data_param` raises a named 400 instead of the 500; next: ask bernt-matthias for the workflow, since how the collection reaches the parameter un-mapped is still open.
- [#23457](https://github.com/galaxyproject/galaxy/issues/23457) — Description: Galaxy's native GCP Batch runner and Pulsar's co-execution client parse CPU/memory and pick machine types independently and have diverged enough to choose different VMs for identical requests, with Galaxy capping at 128 vCPUs instead of rejecting unsupported shapes; next: decide where the shared helper lives before moving code.
- [#23147](https://github.com/galaxyproject/galaxy/issues/23147) — Description: A `SubstitutionError` from `cwl_utils.expression` reaches Sentry uncaught whenever a user-defined tool's command contains a bash command substitution; verified as a real and general bug, not a one-off tool error — `ToolEvaluator._build_command_line` pipes `self.tool.shell_command` straight through `do_eval` (`lib/galaxy/tools/evaluation.py:1138-1143`), which is cwl_utils parameter-reference interpolation, so every `$(...)` in a user tool is parsed as a CWL expression and any shell `$(cmd)` fails to scan; next: decide whether user-tool commands should be interpolated at all versus escaped or opt-in, and surface a tool-level validation error instead of an unhandled exception.
- [#22451](https://github.com/galaxyproject/galaxy/issues/22451) — Description: Sentry `Exception: Failed to extract job.` from workflow extraction; verified still present at `lib/galaxy/workflow/extract.py:232`, where the whole diagnostic (job id, implicit pairs, assoc name) goes to `log.warning` and the raised exception is a bare `Exception` with a message that names nothing — the same opaque-500 shape just fixed for [#21198](https://github.com/galaxyproject/galaxy/issues/21198); next: raise a `MessageException` carrying the diagnostic as a self-contained first step, then treat the unmatched implicit-map output as its own question on the [#22709](https://github.com/galaxyproject/galaxy/issues/22709) extraction surface.
- [#22852](https://github.com/galaxyproject/galaxy/issues/22852) — Description: `test_list_linters` fails against the published `galaxy-tool-util` sdist; the count is a symptom, not the bug — `Linter.list_linters` discovers linters via `import_submodules`, which swallows *every* import failure with `log.exception` and `continue` (`lib/galaxy/util/submodules.py:49-52`), so a linter module that fails to import in a packaged or minimal environment silently deregisters and lint simply skips it with no error anywhere a caller can see; `packages/tool_util/tests/tool_util` symlinks `test/unit/tool_util/`, so downstream packagers run this assertion (currently 148 on dev) against whatever their environment managed to import; next: decide whether linter discovery should fail loudly rather than bumping the number.
- [#23289](https://github.com/galaxyproject/galaxy/issues/23289) — Description: Follow-up review asks from merged PR [#23269](https://github.com/galaxyproject/galaxy/pull/23269); **mostly already fixed on dev and needs re-scoping** — verified that `hasInitialized` is gone and `PairedOrUnpairedListCollectionCreator.vue:459-460` is now literally the `initialize(); watch(() => props.initialElements, reconcileWithInitialElements)` shape the issue asked for, and the silent-removal inconsistency is resolved (both paths toast, with a single toast for a vanished pair and `toastNoLongerAvailable` under strict pairing); all that survives is the title's longer-term ask — lift the by-ID reconciliation seam shared by the three creators into `useCollectionCreator`; next: confirm with John whether to re-scope the issue to the shared composable or close it.
- [#22743](https://github.com/galaxyproject/galaxy/issues/22743) — Description: Playwright coverage for the chat context and docked panel functionality added in [#22096](https://github.com/galaxyproject/galaxy/pull/22096), self-assigned by John; the plan is already written at [[Plan - Chat Context and Docked Panel Tests]] and no blockers surfaced during triage; next: ready to implement straight from the plan.
- [#23339](https://github.com/galaxyproject/galaxy/issues/23339) — Description: `galaxy-test-driver` is published and current on PyPI but cannot boot an embedded Galaxy from wheels — `driver_util.py` resolves its paths against `galaxy_directory()`, which silently returns `cwd.parent.parent` under any site-packages install, and the wheel ships none of the checkout content those paths point at; the config layer already handles wheel installs via `resource_path` and the driver opts out of it; next: decide whether the driver should route through the existing config resolution or the package should stop advertising standalone use.
- [#23077](https://github.com/galaxyproject/galaxy/issues/23077) — Description: A `param_value` filter stops populating its select when both the filter and its `ref` sit two conditionals deep; the reporter's own evidence points at the form build/refresh path rather than evaluation — functional tests pass when values are submitted directly, and flipping which `when` carries `selected="true"` makes the options appear; next: reproduce against the reporter's minimal tool and determine whether the initial tool-form JSON or the client-side refresh drops the filter reference.
- [#22794](https://github.com/galaxyproject/galaxy/issues/22794) — Description: A tool test written deliberately to exercise a failing `validator` now trips the `TestsCaseValidation` linter, so the idiom of testing a validator in action is unlintable; dannon notes such tests may never have actually run (silently skipped), which makes this a question about intent before it is a bug; next: decide whether the test model should be able to declare an expected validation failure — same surface as the `test_case_validation_*` branches.
- [#22534](https://github.com/galaxyproject/galaxy/issues/22534) — Description: There is no way to tell whether a workflow needs updating, and the existing upgrade button does not report what it changed, which makes training and tutorial maintenance guesswork; next: scope against what the refactor/upgrade machinery already computes internally and whether a dry-run diff of tool-version changes can be surfaced without new modelling.

## In progress — branch exists (`wip`)

- [#22437](https://github.com/galaxyproject/galaxy/issues/22437) — Branch `issue_22437_account_interface_enforcement` — Description: `enable_account_interface` gated the UI and the read side of the account API only, so any authenticated user could change their email, username or password, or delete their account through the API and desynchronize from an SSO/OIDC/LDAP directory; state: implemented per [John's draft plan](https://gist.github.com/jmchilton/ca41e57ec1b1d9352a0fed6601e85eaa) as one commit `358ea3d2472` on dev `7524f5e2507`, pushed to `jmchilton`. Four endpoints (`set_information`, `set_password`, `update`, `delete`) now raise `ConfigDoesNotAllowException` (403) for non-admins, guarded in the API controllers rather than `UserManager` because `psa_authnz.sync_user_profile` writes the same fields through the manager precisely when the option is off. Two plan assumptions were corrected against the code: the exception is a 403 not a 400, and the password route is `/password/inputs`; token resets go through `controllers/user.py` so account recovery is unaffected, answering one of the gist's open questions. Per John's call, `PUT /api/users/{user_id}` is guarded per-field so `preferred_object_store_id` stays writable. All four rejections verified red first as genuine 200s, with `err_code` pinned to 403004 because `AuthenticationRequired` is also a 403; 9 + 25 + 13 tests green, lint clean. PR description ready in `vault/reviews/mine/issue_22437_account_interface_enforcement_pr_description.md` and on the clipboard — **not opened**, awaiting John.
- [#21198](https://github.com/galaxyproject/galaxy/issues/21198) — Branch `issue_21198_markdown_label_errors` ([#23608](https://github.com/galaxyproject/galaxy/pull/23608)) — Description: Viewing a `tool_markdown` dataset whose report references an `output=`/`input=` label the creating job lacks returns an opaque 500 from `resolve_job_markdown`, which raised a bare `Exception("Unknown exception")` for outputs and let a `KeyError` escape for inputs; state: fixed as one commit `0dd2e5991bd` on **`release_26.1`** `4e33e333dab` and pushed to `jmchilton` — redone off the release branch since it is a bug fix, and the buggy code is byte-identical on 26.1 and dev; both branches now route through a `_resolve_job_reference` helper raising `MalformedContents` (400) that names the bad label and lists the valid ones, following `WorkflowInvocation.get_output_object`'s precedent; confirmed distinct from #22840 — same `DatasetsService.report` function, but #21198 raises one line earlier and never reaches the tuple unpack, which is itself a dev-only regression so #23599 needs no backport; two API tests verified red at 500 on 26.1 then green, unit tests from the first pass dropped at John's request; opened as a draft PR on 2026-09-20 against `release_26.1` from `vault/reviews/mine/issue_21198_markdown_label_errors_pr_description.md`, monitoring CI before undrafting.
- [#22705](https://github.com/galaxyproject/galaxy/issues/22705) — Branch `issue_22705_job_icj_id` ([#23606](https://github.com/galaxyproject/galaxy/pull/23606)) — Description: The Job detail API hides `implicit_collection_jobs_id` even though the model association exists, forcing consumers to rebuild map-over grouping from the outside; state: implemented per [the verified plan](ISSUE_22705_PLAN.md) as one atomic commit on dev `48c7d7028fc`, pushed to `jmchilton` at `a672d44b8eb`; subagent review applied before squashing — `/api/jobs/search` covered, agent id encoding fixed, and the `test_model.py` scoping guard dropped at John's request; the three field tests and the agent-encoding assertion were each verified red first, full `test_workflow_extraction.py` 65 passed 1 skipped, client schema regenerated and `pnpm type-check` clean; opened as a draft PR on 2026-09-20 from `vault/reviews/mine/issue_22705_job_icj_id_pr_description.md`, monitoring CI before undrafting.
- [#22840](https://github.com/galaxyproject/galaxy/issues/22840) — Branch `issue_22840_dataset_report` ([#23599](https://github.com/galaxyproject/galaxy/pull/23599)) — Description: `GET /api/datasets/{id}/report` unpacks two values from the three-tuple returned by `ready_galaxy_markdown_for_export`, so the endpoint 500s for every `tool_markdown` dataset on `dev`; state: fixed and committed as `080b21ead2f` on dev `48c7d7028fc` and pushed to `jmchilton`; API test verified red at 500 and green at 200, framework tool tests green; opened as a draft PR on 2026-09-19, monitoring CI before undrafting.
- [#23544](https://github.com/galaxyproject/galaxy/issues/23544) — Branch `issue_23544_collection_output_assertions` ([#23554](https://github.com/galaxyproject/galaxy/pull/23554)) — Description: Framework workflow tests ignore `element_tests` when detecting collection outputs and the test model rejects nested `count`; state: out of draft, 59 checks green with one unrelated LocalRunner failure.
- [#23579](https://github.com/galaxyproject/galaxy/issues/23579) — Branch `issue_23579_annotation_indexes` — Description: PostgreSQL B-tree indexes on annotation text reject long values and can 500 workflow imports; state: mvdbeek agreed to dropping the indexes plus a 64KB annotation limit, both committed, rebased on `9ba67b81398`, pushed to `jmchilton`; PR description ready in `vault/reviews/mine/issue_23579_annotation_indexes_pr_description.md`, awaiting CI before opening.
- [#23424](https://github.com/galaxyproject/galaxy/issues/23424) — Branch `issue_23424_when_expression_validation` — Description: Validate `when` expression input references at workflow import; state: triage the Mulled, Playwright, Selenium, and package failures on `2938c2448c6`, then open using `WHEN_VALIDATION_PR_DESCR.md`.
- [#21788](https://github.com/galaxyproject/galaxy/issues/21788), [#21789](https://github.com/galaxyproject/galaxy/issues/21789) — Branch `fix_copied_datasets` ([#21806](https://github.com/galaxyproject/galaxy/pull/21806)) — Description: Workflow extraction produces an empty workflow for empty collections and fails to connect tools for dynamically-created nested collections; state: draft needing merge-conflict resolution and triage of eight red checks.
- [#21971](https://github.com/galaxyproject/galaxy/issues/21971) — Branch `wf_tool_state` ([#22996](https://github.com/galaxyproject/galaxy/pull/22996)) — Description: Schema-aware workflow tool state validation and round-trip fidelity; state: draft needing merge-conflict resolution and triage of two red checks. The issue-to-PR link is inferred from scope, not declared on either.
- [#22710](https://github.com/galaxyproject/galaxy/issues/22710) — Branch `workflow_state_backfill` — Description: Workflow invocations never capture validated tool request state, so graph view ([#21659](https://github.com/galaxyproject/galaxy/issues/21659)) and better history extraction ([#22709](https://github.com/galaxyproject/galaxy/issues/22709)) have to reconstruct it from invocation tables; guerler argued for minting `ToolRequest` rows (MINT), John for a shared `ToolExecutionState` referenced by both `ToolRequest` and `WorkflowInvocationStep` (EXEC_STATE), mvdbeek agreed the real requirement is capturing non-tool steps and decision points; state: a worktree exists at `1f87eebce32` with uncommitted changes and is **not** tracked in `vault/reviews/mine/index.md`; John spent four days across five migrations and reported the attempt failed, so the branch is evidence rather than a candidate to push — next: decide whether to restart from the MINT increment or salvage the TES-keyed shape on the branch.

## Blocked (`blocked`)

- [#23428](https://github.com/galaxyproject/galaxy/issues/23428) — Branch `workflow_input_pipe_names` — Description: Decide whether pipes are allowed in subworkflow input names; blocked on: choosing validation-only, top-level correction, or nested correction from `workflow_input_pipe_names_versions.md`.
- [#23444](https://github.com/galaxyproject/galaxy/issues/23444) — Description: Settle the canonical nested parameter-reference syntax for YAML/user-defined tools before adding model-level `format_source`/`metadata_source` validation, so runtime, linting, and Pydantic validation stop diverging; blocked on: John answering mvdbeek's two direct questions on the scoping comment — whether literal input names containing path punctuation need supporting at all (are there any in the IUC?), and whether `collection.forward` should be the preferred spelling over `collection["forward"]`, supporting both but asking for the former. Answering unblocks implementation.
- [#23183](https://github.com/galaxyproject/galaxy/issues/23183) — Description: Request to keep a warm pool of pre-initialized interactive tool instances so Jupyter/RStudio start in seconds; blocked on: startup performance metrics — John and mvdbeek both want to know which part of startup is actually slow before breaking Galaxy's permission, database, and job communication model for a pool, and bgruening measures ~30s warm starts on EU against the reported "several minutes", so the premise is environment-specific. mschatz wants all options kept open.
- [#22598](https://github.com/galaxyproject/galaxy/issues/22598) — Description: Toolshed repo contents and changelog were disabled for anonymous users after bot traffic overwhelmed the server, so tool XML can no longer be browsed without logging in; blocked on: an infrastructure decision rather than code — mvdbeek notes rate limiting alone does not help against botnets, natefoo suggests either dedicated TS routes for the content worth exposing anonymously or a CDN/captcha DDoS mitigation. Not ours to pick up unilaterally.

## Large Project Scoped — too big for triage agent to work on (`large`)

- [#22739](https://github.com/galaxyproject/galaxy/issues/22739) — Description: Extract `Tool.to_json` into a `ToolFormBuilder` module with richer types and a clear boundary against legacy `basic.py`; the plan is published at <https://jmchilton.github.io/galaxy-brain/plans/toolformpopulaterefactor/> and needs agreement before any code moves.
- [#22200](https://github.com/galaxyproject/galaxy/issues/22200) — Description: Workflow semantics documentation plus test mapping; tracked as its own vault project at [[workflow_semantics]].
- [#22709](https://github.com/galaxyproject/galaxy/issues/22709) — Description: Meta-issue for the workflow extraction overhaul (notebook to graph to workflow); individual extraction bugs are tracked separately above.
- [#21659](https://github.com/galaxyproject/galaxy/issues/21659) — Description: Make progress towards a History Graph View.
- [#22954](https://github.com/galaxyproject/galaxy/issues/22954) — Description: Format 2 workflows have no practical layout round-trip — hand-authored YAML imports as a 10px diagonal and editor layout cannot be saved back to source; spans gxformat2, the editor, and the IWC authoring model.
- [#22999](https://github.com/galaxyproject/galaxy/issues/22999) — Description: Flag workflow outputs as ephemeral so the metadata setter is skipped for pure glue-code steps, and to give a future "workflow compiler" the information it needs to fuse or stream steps; spans the workflow model, format2, and the scheduler, and pvanheus notes deferred datasets already skip metadata and may be the reusable mechanism.

## Done — kept for the branch cross-reference (`done`)

_Prune entries when the corresponding worktree is torn down._
