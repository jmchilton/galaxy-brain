# Open PR and branch queue

GitHub state last refreshed: 2026-09-16.

## Open Galaxy PRs — ready from our side (`ready`)

- [#23229](https://github.com/galaxyproject/galaxy/pull/23229) — branch `data_validation` — Description: Adds repository linting for data-manager and reference-data bundles plus a standalone CLI; blockers: none, two unrelated CI checks are red.
- [#23334](https://github.com/galaxyproject/galaxy/pull/23334) — branch `pja_warn_unsupported_mapped_over` — Description: Hides job-only Email and Output cleanup actions on pick_value steps and warns for saved configurations; blockers: none, settled CI on `e35ed98431` has a wheel-index uvloop 503 before PostgreSQL tests and the API test_delete_job_with_message failure explicitly marked known transient #21242; 5 backend and 13 client tests passed locally.
- [#23340](https://github.com/galaxyproject/galaxy/pull/23340) — branch `review-nits` — Description: Collects small review fixes for redirects, repeat cloning, data tables, and visualizations; blockers: none, four unrelated CI checks are red.
- [#23482](https://github.com/galaxyproject/galaxy/pull/23482) — branch `color_schema` — Description: Accepts missing or null workflow-comment colors and canonicalizes them to `none`; blockers: awaiting Marius's confirmation, no known author-side work; John [commented that the sole API failure appears unrelated](https://github.com/galaxyproject/galaxy/pull/23482#issuecomment-5699561665), all other checks have no failures, and the schema-package regression is fixed.
- [#23436](https://github.com/galaxyproject/galaxy/pull/23436) — branch `22087-finishing-state-fixes` — Description: Adds a FINISHING job state and restart-safe Pulsar recovery for interrupted metadata; blockers: none, now out of draft with CLEAN merge status and settled CI without failures on `b9aec6bcdc`; John has manually posted the review reply.

## Open Galaxy PRs — needs attention (`attention`)

- [#23433](https://github.com/galaxyproject/galaxy/pull/23433) — branch `pick_value_input_readiness` — Description: Makes pick-value steps wait for every connected input before selecting runtime values; blockers: settle the paused/remapping-test design with Marius in one unresolved but already-answered review thread; unblocks #23330 and #23455.
- [#22860](https://github.com/galaxyproject/galaxy/pull/22860) — branch `extract_next` — Description: Extracts reproducible workflows and narrative reports from Galaxy notebook activity; blockers: wait for CI on `0e87eb2663`; rebased onto freshly fetched dev `93d70cf46ed`, with 95 backend and 72 client tests passing locally.

## Draft Galaxy PRs — ready to undraft (`draft_ready`)

- [#23554](https://github.com/galaxyproject/galaxy/pull/23554) — branch `issue_23544_collection_output_assertions` — Description: Detects bare element tests and permits nested collection size assertions; blockers: none, CI on `acbd6cf58e` has settled with 59 successful checks and one unrelated API failure caused by LocalRunner failing to open a temporary stderr file with OSError Bad file descriptor; ready to undraft, with 20 focused tests passing locally.

## Draft Galaxy PRs — waiting for CI (`ci_wait`)

- [#23565](https://github.com/galaxyproject/galaxy/pull/23565) — branch `toolshed_stock_trs` — Description: Publishes stock-tool TRS metadata and versions for automatic version discovery; blockers: finish replacement PR and fork CI on `3d2873a811` after fixing the test mypy error.
- [#23559](https://github.com/galaxyproject/galaxy/pull/23559) — branch `manager_app_job_services` — Description: Initializes Celery manager job services and requires an error-reporting implementation through mypy; blockers: finish five pending integration/browser jobs on `6e19f2e81d`, with 51 successful checks and no failures so far including green Python lint/type checking; targeted mypy red-to-green and 25 existing unit tests passed locally, with [the description retained here](manager_app_job_services_pr_description.md).

## Draft Galaxy PRs — needs author work (`author_work`)

- [#23455](https://github.com/galaxyproject/galaxy/pull/23455) — branch `pick_value_first_ok_or_skip` — Description: Adds a pick-value mode that ignores failed or null inputs and skips without usable values; blockers: depends on #23433 and has three completed red CI checks.
- [#23330](https://github.com/galaxyproject/galaxy/pull/23330) — branch `pja_guard_mapped_over_skipped` — Description: Prevents post-job actions from applying to skipped mapped-over workflow outputs; blockers: approved but depends on #23433 and has one completed red CI check.
- [#23326](https://github.com/galaxyproject/galaxy/pull/23326) — branch `htcondor_pulsar` — Description: Shares HTCondor mechanics with Pulsar and bounds held-job handling with a grace window; blockers: Pulsar #486 needs rebase and resolution of its hold/retry-policy thread.
- [#22996](https://github.com/galaxyproject/galaxy/pull/22996) — branch `wf_tool_state` — Description: Adds gxwf CLI tooling for static validation and conversion of stateful Galaxy workflows; blockers: resolve merge conflicts and triage two completed red CI checks.
- [#22988](https://github.com/galaxyproject/galaxy/pull/22988) — branch `notebook_iframe` — Description: Explores embedded Galaxy notebooks in Loom with automatic synchronization; blockers: resolve merge conflicts and triage failing CircleCI.
- [#21806](https://github.com/galaxyproject/galaxy/pull/21806) — branch `fix_copied_datasets` — Description: Fixes workflow extraction when datasets were copied across histories; blockers: resolve merge conflicts and triage eight completed red CI checks.
- [#21199](https://github.com/galaxyproject/galaxy/pull/21199) — branch `test-stories` — Description: Adds executable tutorial stories for Galaxy developers and automated documentation; blockers: resolve merge conflicts and triage two completed red package checks.
- [#19937](https://github.com/galaxyproject/galaxy/pull/19937) — branch `shed_redirect` — Description: Redirects a legacy Tool Shed URL pattern for Galaxy 26.0 compatibility; blockers: rebase and triage nine completed red CI checks.
- [#15868](https://github.com/galaxyproject/galaxy/pull/15868) — branch `typed_exceptions` — Description: Strengthens typing around Galaxy exception classes and their consumers; blockers: approved but needs merge-conflict resolution and current CI.
- [#12238](https://github.com/galaxyproject/galaxy/pull/12238) — branch `collection_restructure_panel` — Description: Adds a panel for transforming dataset collection layouts; blockers: needs merge-conflict resolution and current CI.
- [#9911](https://github.com/galaxyproject/galaxy/pull/9911) — branch `pulsar-mq-pull` — Description: Polls for Pulsar MQ jobs potentially missed by normal event handling; blockers: needs merge-conflict resolution, current CI, and resolution of its old review thread.
- [#8846](https://github.com/galaxyproject/galaxy/pull/8846) — branch `job_files_refactor` — Description: Refactors the job-files API toward a microservice-compatible boundary; blockers: needs merge-conflict resolution and current CI.

## Galaxy branches — needs a PR (`branches_need_pr`)

- Branch `app_import_cost` — Description: Implements Phase 0 by deferring agent and Celery imports and removing the eager Pulsar staging import; blockers: choose a standalone PR or deliver through `metadata_lazy_imports`, which already includes these changes; [pushed branch](https://github.com/jmchilton/galaxy/tree/app_import_cost), with temporary verification tests removed as requested.
- Branch `metadata_lazy_imports` — Description: Combines Marius’s metadata lazy imports with Phase 0 while preserving older Python and current security checks; blockers: check fork CI and prepare a PR; [pushed branch](https://github.com/jmchilton/galaxy/tree/metadata_lazy_imports) at `da65a339fb7`, with his 18 commits rebased onto dev `07f42d2a5a5` and Phase 0 merged, 819 tests passing with 20 skips and one expected failure plus 10 native Python 3.15 checks passing.
- Branch `pulsar_remote_job_directory` — Description: Reads the Pulsar-side job and tool directories from the reported job config instead of re-deriving them with `os.path.abspath`, and documents the `__PULSAR_JOBS_DIRECTORY__` / `__PULSAR_JOB_DIRECTORY__` tokens beside `jobs_directory` in both job_conf samples; blockers: wait on fork CI for `c7f98f8f4b6` (35 workflows queued at push), then open against `dev` using [the prepared description](pulsar_remote_job_directory_pr_description.md); the Galaxy half of [Pulsar #29](https://github.com/galaxyproject/pulsar/issues/29) and companion to [Pulsar #512](https://github.com/galaxyproject/pulsar/pull/512), independent of it in either order, with 13 unit tests passing locally red-to-green and the second (docs) commit droppable if it should wait for #512.
- Branch `playwright_rendering_rules_workflow_1` — Description: Drops the stale `selenium_only` decorator from `test_rendering_rules_workflow_1`, which passes under Playwright unchanged; blockers: no fork CI yet on `4287ccb92a9`; single-line diff, passed 3/3 locally under Playwright, Selenium backend unverified locally (needs Xvfb, macOS can't).
- Branch `test_tool_conf_randomlines_symlink` — Description: Adds the missing `for_workflows/randomlines.xml` symlink for a `sample_tool_conf.xml` entry that has dangled since the `samples_tool_conf` rename, mirroring `dccab49baa8` for catWrapper; blockers: no fork CI yet on `18f5bfb276c`; not needed by `-selenium`/`-playwright` CI (both also load `tool_conf.xml.sample`) but removes a startup error for `-framework` runs and local `GALAXY_RUN_WITH_TEST_TOOLS=1`.
- Branch `playwright_column_definition_send_enter` — Description: Stops sending ENTER to the column-definition description `<textarea>`, where it inserted a literal newline into the saved value, and drops `selenium_only` from `test_collection_input_sample_sheet_chipseq_example`; blockers: no fork CI yet on `2675c865001`; the field is a `v-model` FormElement that commits on every input event so the ENTER was never needed, and the test passes locally under both backends.
- Branch `playwright_gesture_vocabulary_port` — Description: Replaces 13 `action_chains()` call sites with the `hover()` / `move_to_and_click()` gestures the protocol already exposes, removing three `backend_type` branches and letting `test_conditional_subworkflow_step` run under Playwright; blockers: no fork CI yet on `eca1d4639c0`; no new API, and locally green under both backends for the conditional-subworkflow, workflow-rename, and window-manager paths.
- Branch `playwright_neutral_key_press` — Description: Adds a `press(*keys, modifiers=...)` gesture to `HasDriverProtocol` with native Selenium and Playwright implementations plus a backend-neutral `Key` enum, and routes `send_enter`/`send_escape`/`send_backspace` through it; blockers: no fork CI yet on `5afd62b297b`; also fixes a latent no-op (page-level `send_escape()` built an ActionChains but never called `perform()` under Selenium), which changes behavior for `test_workflow_run_target` and wants CI confirmation; 21 new dual-backend unit tests, 400 passing in `test/unit/selenium/`.
- Branch `toolshed_delegate_publishing` — Description: Allows push collaborators to update Tool Shed metadata and shows the repository owner in push access; blockers: finish queued jmchilton CI on `37d06ffc1e4`, then prepare the description and open a PR; rebased onto dev `07f42d2a5a5` with an explicit commit message about the permission change and 13 focused API tests passing again, with [Tool Shed CI tracked here](https://github.com/jmchilton/galaxy/actions/runs/35134282289).
- Branch `test_case_validation_state_errors` — Description: Replays #23002 with current test-case state validation hardening; blockers: obtain current fork CI on `262e080dfe` before opening the base PR against `dev` using [the prepared description](test_case_validation_state_errors_pr_description.md); pushed after rebase onto dev `c6c3b6df49f`, with 24 focused tests passing, but no branch CI runs are currently listed.
- Branch `test_case_validation_model_parse_errors` — Description: Replays #23004 with tool-model parse reporting and case-insensitive Infinity bounds; blockers: finish 21 still-queued fork workflows on `b70aee49dd`, then open a stacked PR using [the prepared description](test_case_validation_model_parse_errors_pr_description.md); pushed after rebase onto `test_case_validation_state_errors`, with 26 focused tests passing and no completed non-skipped workflows yet.
- Branch `when_expression_analysis` — Description: Matches workflow `when` inputs structurally instead of by substring; blockers: triage four completed fork CI failures, then open using `OPT_INPUT_PR_2_DESCR.md`.
- Branch `issue_23424_when_expression_validation` — Description: Rejects obsolete pipe-delimited tool-input paths during workflow import; blockers: triage Mulled, Playwright, Selenium, and package failures on `2938c2448c6`, then open using `WHEN_VALIDATION_PR_DESCR.md`.
- Branch `optional_input_gating` — Description: Adds authoring and validation for running steps only when optional inputs are present; blockers: triage five completed fork CI failures, then open using `OPT_INPUT_PR_3_DESCR.md`.
- Branch `workflow_input_pipe_names` — Description: Reserves pipes in input names, with validation-only, top-level correction, and nested correction alternatives; blockers: fix five mypy errors from name-mangled private-method calls and isort ordering in test_workflow_input_names for the validation-only and top-level variants, finish remaining CI, and choose a scope from [the comparison note](workflow_input_pipe_names_versions.md); nested variant has no failures yet but 12 workflows remain pending, and no variant rewrites expressions.
- Branch `cwl_fixes_5` — Description: Collects CWL validation, typing, output-path, and job-document compatibility fixes; blockers: rebase, replay subworkflow mapping, run conformance tests, and decide the PR split.
- Branch `subworkflow_mapping` — Description: Implements ordered-axis mapping and delayed pass-through for callable subworkflows; blockers: preserve four modified files, rebase 14 commits onto `dev`, validate, and open a PR.
- Branch `subworkflow_mapping_when_alignment` — Description: Aligns mapped subworkflow when-values with compatible collection inputs; blockers: determine its stack relationship, rebase, validate, and open a PR.
- Branch `parameter_model_optional_agreement` — Description: Aligns genome-build and drill-down optionality with focused coverage; blockers: rebase and run local validation before opening a PR.

## Open PRs in other projects (`other_prs`)

- [Pulsar #486](https://github.com/galaxyproject/pulsar/pull/486) — branch `htcondor2` — Description: Adds a queued HTCondor manager using shared Galaxy/Pulsar mechanics; blockers: rebase and resolve the hold/retry-policy review thread before Galaxy #23326.

- [IWC #1366](https://github.com/galaxyproject/iwc/pull/1366) — branch `codex/expand-claude-review-command` — Description: Expands the agent review command for IWC workflows; blockers: required review, CI has no failing checks.
- [IWC #1365](https://github.com/galaxyproject/iwc/pull/1365) — branch `deploy-report-only-on-failure` — Description: Reports deployment status only when deployment actually failed; blockers: required review, CI has no failing checks.
- [Planemo #1695](https://github.com/galaxyproject/planemo/pull/1695) — branch `issue-1693-iwc-missing-tests-error` — Description: Requires workflow tests under the IWC lint profile; blockers: required review, one CI job failed on Zenodo 504s and training-test fallout; #1698 addresses outage handling.
- [Planemo #1698](https://github.com/galaxyproject/planemo/pull/1698) — branch `skip-tests-when-zenodo-down` — Description: Skips Zenodo-dependent tests when its API is unavailable; blockers: required review, CI has no failing checks.
