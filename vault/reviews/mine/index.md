# Open PR and branch queue

GitHub state last refreshed: 2026-09-20.

## Open Galaxy PRs — ready from our side (`ready`)

- [#23229](https://github.com/galaxyproject/galaxy/pull/23229) — branch `data_validation` — Description: Adds repository linting for data-manager and reference-data bundles plus a standalone CLI; blockers: none, settled CI on `020e16cffb` has 54 successful checks and two previously diagnosed unrelated reds in Converter tests and Playwright shard 2.
- [#23334](https://github.com/galaxyproject/galaxy/pull/23334) — branch `pja_warn_unsupported_mapped_over` — Description: Hides job-only Email and Output cleanup actions on pick_value steps and warns for saved configurations; blockers: none, settled CI on `e35ed98431` has a wheel-index uvloop 503 before PostgreSQL tests and the API test_delete_job_with_message failure explicitly marked known transient #21242; 5 backend and 13 client tests passed locally.
- [#23340](https://github.com/galaxyproject/galaxy/pull/23340) — branch `review-nits` — Description: Collects small review fixes for redirects, repeat cloning, data tables, and visualizations; blockers: none, settled CI on `ca8a44ed82` has 56 successful checks and four previously diagnosed unrelated reds in Unit tests 3.14, Integration Selenium, Integration shard 1, and Playwright shard 2.
- [#22860](https://github.com/galaxyproject/galaxy/pull/22860) — branch `extract_next` — Description: Extracts reproducible workflows and narrative reports from Galaxy notebook activity; blockers: none, CLEAN and fully settled with 61 successful checks and no failures on `0e87eb2663`; 95 backend and 72 client tests passed locally.
- [#23574](https://github.com/galaxyproject/galaxy/pull/23574) — branch `playwright_neutral_key_press` — Description: Adds neutral key presses and active-element access, and migrates the ARIA connection-menu test; blockers: watch CI on `b961b619af2`, rebased onto dev `48c7d7028fc` on 2026-09-19 and force-pushed as `29018d7cd8a`, which clears the single red on `92c508f315f` — Selenium shard 1's sample-sheet column-description trailing-newline assertion, fixed by #23567 (merge `874e8a62830`) and confirmed present in dev; the five commits replayed without conflict and the diff was unchanged at 12 files and 530 added lines, with 61 `test_keys.py` and 348 `test_has_driver.py` unit tests passing locally after the rebase, but the rebased run went red on Python linting with one F401 the rebase created — dev had meanwhile removed the other `selenium_only` decorators in `test_workflow_editor.py` while this branch removed the last one, leaving the import unused — fixed in `b961b619af2` with `ruff check .` clean on the whole tree; [the prepared description is here](playwright_neutral_key_press_pr_description.md).

- [#23593](https://github.com/galaxyproject/galaxy/pull/23593) — branch `csv_col_assertions_delimiter` — Description: Splits column assertions on the dataset's delimiter metadata instead of the declared ftype, gated on profile 26.2; blockers: none, undrafted on 2026-09-19; CI on `4eb5ba819dd` has 57 successful checks and one API tests shard 1 failure John judged transient; rescues closed #21391 by preserving d-callan's eight commits squashed with her authorship onto dev `101e060a65c`, moving her 26.0 gate to 26.2 because dev is 26.2.dev0, and reading `metadata_delimiter` off the dataset rather than inferring a comma from `ftype="csv"`, which also covers tests that declare no ftype; six new unit tests were verified red-to-green and 275 verify-related unit tests pass; the first CI run was red on the `column_assertion_delimiter` framework tool, which read the csv output as one column because `adapt_tool_source_dict` never copied `profile` from `ToolTestDict` into `ToolTestDescriptionDict` — d-callan's commit threaded the profile everywhere except that bridge, so `ToolTestDescription.profile` was always `None` and every profile gate in the verification path was dead — fixed in `4eb5ba819dd` with a red-to-green unit test in `test_test_definition_parsing.py` and all three framework tool tests now passing locally against a copy-on-write clone of another worktree's venv, and the workflow and Selenium verification paths deliberately keep the tab default since neither has a tool profile to gate on, with [the design and verification notes here](csv_col_assertions_delimiter_notes.md).

- [#23600](https://github.com/galaxyproject/galaxy/pull/23600) — branch `test_case_validation_state_errors` — Description: Reports test-case state-building failures as per-test validation errors instead of aborting the whole tool; blockers: none, undrafted on 2026-09-20; settled CI on `262e080dfe0` has 57 successful checks and one red Integration shard 0 where four `TestSingularityJobsIntegration` tests failed on a Docker Hub `TOOMANYREQUESTS` unauthenticated pull-rate limit fetching `busybox:1.36.1-glibc`, which is infrastructure and nowhere near this tool-test validation diff; opened as a draft on 2026-09-19 against dev with the prepared description plus the repo's standard testing/license sections, and the `@richard-burhans` credit written without the at-sign so opening it pinged nobody — say the word and I'll add the mention; replays #23002 with the original commit authorship preserved on `e4513d389b0`, 24 focused tests pass locally, and dev has moved 170 commits ahead of the branch base so a rebase is likely wanted before it goes ready for review; the companion branch `test_case_validation_model_parse_errors` stacks on top and still needs its own PR.

- [#23599](https://github.com/galaxyproject/galaxy/pull/23599) — branch `issue_22840_dataset_report` — Description: Fixes the 500 on `GET /api/datasets/{id}/report` for `tool_markdown` datasets; blockers: none, undrafted on 2026-09-20; settled CI on `080b21ead2f` has 60 successful checks and one red Integration Selenium, `test_history_import_export_ftp.py::test_history_export_tracking` timing out on `.go-next-btn`, the same unrelated history-export flake already recorded against `test_case_validation_model_parse_errors` and untouched by this three-file diff; opened as a draft on 2026-09-19 against dev `48c7d7028fc`, using [the prepared description](issue_22840_dataset_report_pr_description.md); `report()` unpacked two values from the three-tuple `ready_galaxy_markdown_for_export` has returned since `689aaf013d`, and now returns the embed-expanded markdown as `managers/pages.py` does for display content, since `ToolReport.vue` renders read-only and has no editor variant; one API test was verified red at 500 and green at 200, and all three `markdown_report_*` framework tool tests pass after the fixture gained an embedded `${galaxy generate_galaxy_version()}` directive — without it every report fixture used only fenced blocks, for which the two markdown variants are byte-identical, so the test could not tell the expanded variant from the plain one; the tests ran on SQLite in a worktree whose Playwright browser install failed on DNS, so it is not E2E-ready.

- [#23594](https://github.com/galaxyproject/galaxy/pull/23594) — branch `issue_23579_annotation_indexes` — Description: Drops whole-text annotation indexes that PostgreSQL cannot hold and bounds annotation length at 64KB; blockers: none, undrafted on 2026-09-20; CLEAN and fully settled on `32d3ac4efa5` with 56 successful checks, 2 skipped, and nothing red or pending — the Test Galaxy release script red predicted below never materialized on this run; restructured on 2026-09-19 at a reviewer's preference into two commits, `a6be232a362` carrying the migration alone and `32d3ac4efa5` squashing the model change, test tightening, and mypy fix into one atomic commit with no archeology, verified byte-identical to the pre-restructure tree and force-pushed; `test/unit/data/model/migrations/test_migrations.py` still rides in the second commit and could move into the migration commit if the reviewer wants the migration's test travelling with it; the fresh CI on `32d3ac4efa5` is settled and CLEAN with 55 successful checks, 2 skipped, and nothing red — the Test Galaxy release script red predicted below did not materialize on this run; opened as a draft on 2026-09-18 against dev; the first run was red on Python linting with one mypy error of mine — `__subclasses__()` yields `type[ItemAnnotationAssociation]` and the mixin is unmapped, so `__table__` is invisible — fixed by sweeping `Base.metadata` for annotation columns instead, which is typed and also catches an annotated table added without the mixin, verified red-to-green and with the error reproduced locally under the repo `mypy.ini` before and after; mvdbeek agreed to both halves in #23579, the limit is one `@validates` on a new `ItemAnnotationAssociation` mixin covering all eight annotation tables and raising `RequestParameterInvalidException` so oversized annotations return 400 rather than 500, and the test suite was tightened before opening — 103 added lines instead of 149, the migration test cut from four database cycles to one and re-verified red-to-green against a stubbed upgrade, with 13 model unit tests and 3 workflow API tests passing locally; no PostgreSQL server was available locally so the new tests ran on SQLite, though the limit is enforced in Python and dialect-independent, and the one red to expect is Test Galaxy release script, which fails on every jmchilton branch because the fork’s `master` is frozen at 2019 with `VERSION_MINOR = None` while the same workflow is green on every upstream dev run, with [the prepared description here](issue_23579_annotation_indexes_pr_description.md).

- [#23602](https://github.com/galaxyproject/galaxy/pull/23602) — branch `playwright_unlock_uploads` — Description: Runs 16 of 17 upload tests under Playwright by dropping 15 `selenium_only` decorators, porting a raw `action_chains`; blockers: none, undrafted on 2026-09-20; CLEAN and fully settled on `4d2fc929406` with 23 successful checks and nothing red or pending; off dev `48c7d7028fc`, 1 file with 5 insertions and 22 deletions. Three rule-builder tests failed on `_scroll_to_end_of_table`, which built a raw move/click/fifteen-keys chain; `move_to_and_click()` and `send_keys_to_page()` already cover it on both backends and the keys now go in one `perform` instead of fifteen. `test_rules_example_3_list_pairs` keeps its decorator with a real diagnosis replacing the blanket reason — in `rule_builder_swap_columns` nothing blurs the second column selector, so its closing dropdown (`.multiselect__content-wrapper.multiselect-leave-active`) intercepts the editor's Apply button; `send_escape` on the widget's input clears `multiselect--active` and the button is hit-testable ~500ms later, yet the click still times out over 30s of retries, so the cause is diagnosed but not solved. Verified 16 passed and 1 skipped under Playwright and the three ported tests under Selenium; `test_rules_example_5_matching_collections` fails locally under Selenium with an `ElementClickInterceptedException` at (858, 849) on dev's own unmodified file too, so that is a local viewport artifact rather than a regression, and [the prepared description is here](playwright_unlock_uploads_pr_description.md).

- [#23603](https://github.com/galaxyproject/galaxy/pull/23603) — branch `playwright_unlock_workflow_management` — Description: Runs 12 of the 13 workflow-management tests under Playwright by dropping their blanket `selenium_only` decorators; blockers: none, undrafted on 2026-09-20; CLEAN and fully settled on `66aa76fc613` with 23 successful checks and nothing red or pending; off dev `48c7d7028fc`, 1 file with 2 insertions and 14 deletions, and all twelve pass unmodified. `test_view` keeps its decorator with the cause recorded rather than the blanket reason — it asserts an external link opens in a second tab via `driver.switch_to.window`, and `NavigatesGalaxy.driver` raises `NotImplementedError` under Playwright (`navigates_galaxy.py:286`) because there is no backend-neutral window/tab vocabulary yet; Playwright models tabs as `page.context.pages` rather than Selenium's window handles, so that is its own piece of work. Verified 12 passed and 1 skipped under Playwright; no Selenium run was needed because the branch changes no shared code and `selenium_only` only skips under Playwright, and [the prepared description is here](playwright_unlock_workflow_management_pr_description.md).

- [#23433](https://github.com/galaxyproject/galaxy/pull/23433) — branch `pick_value_input_readiness` — Description: Makes pick-value steps wait for every connected input before selecting runtime values; blockers: none from our side, awaiting mvdbeek on the one unresolved review thread at `test_workflows.py:3454` — they asked on 2026-09-03 whether the paused/remapping test discriminates, John answered the same day with a reworked test that inspects the invocation before rerunning and offered to drop both weaker tests in favour of the blocking test in #23330, and there has been no reply in the 17 days since; settled CI on `0ab6403930` has 55 successful checks and one red Integration shard 0; unblocks #23330, which is already approved, and #23455.

- [#23598](https://github.com/galaxyproject/galaxy/pull/23598) — branch `playwright_unlock_histories_list` — Description: Runs 13 of the 14 saved-histories-list tests under Playwright by dropping their blanket `selenium_only` decorators; blockers: none, undrafted on 2026-09-20; settled CI on `0ba200bd303` has 22 successful checks and nothing red, with one check re-queued by the undraft; mergeable; opened as a draft on 2026-09-19 against dev `d5207e343c7`, a single unstacked commit of 1 insertion and 14 deletions, with all 13 re-verified passing locally under Playwright against that dev; five of them depended on #23589 (merged 2026-09-18) replacing `action_chains()` with `hover()` in `select_history_card_operation` (`navigates_galaxy.py:807`), and `test_tags` keeps its decorator now carrying the diagnosed cause (its tag editor never renders, so `.stateless-tags button` is never found) rather than the blanket reason.
- [#23601](https://github.com/galaxyproject/galaxy/pull/23601) — branch `playwright_unlock_tool_form` — Description: Runs 24 tool-form tests under Playwright by dropping 19 `selenium_only` decorators and fixing the race they hid; blockers: none, undrafted on 2026-09-20; settled CI on `0739e737fe7` has 36 successful checks and nothing red, with one check re-queued by the undraft; opened as a draft against dev on 2026-09-19 and mergeable; off dev `48c7d7028fc`, 2 files with 5 insertions and 20 deletions. Seven tests failed on the first Playwright pass and all seven had one cause — `table#tool-parameters` and the dataset-details/job-outputs tables become visible with only their header, so the `tbody` snapshot in `_get_dataset_tool_parameters` and `_table_to_key_value_elements` came back empty; Selenium's slower round-trips hid it. Both helpers now wait for a row first, using a new `dataset_details.tool_parameters_row` selector in `navigation.yml`. Verified 24/24 under Playwright on the full file and 8/8 under Selenium on the touched tests, so this is a real race fix rather than a backend gap, and two failures I initially attributed to a `Select` wrapper gap and a collection-element timeout were cascades from the earlier failures in the same run; [the prepared description is here](playwright_unlock_tool_form_pr_description.md).

## Draft Galaxy PRs — ready to undraft (`draft_ready`)

_Empty — #23592 merged at `7524f5e2507` and the other two were undrafted._

## Draft Galaxy PRs — waiting for CI (`ci_wait`)

- [#23608](https://github.com/galaxyproject/galaxy/pull/23608) — branch `issue_21198_markdown_label_errors` — Description: Turns an unresolvable `output=`/`input=` label in a `tool_markdown` report into a 400 naming the label instead of an opaque 500; blockers: monitor CI on `0dd2e5991bd` — opened as a draft by John on 2026-09-20 against **`release_26.1`**, mergeable, with 42 checks still pending at open; fixes [#21198](https://github.com/galaxyproject/galaxy/issues/21198), one commit on `release_26.1` `4e33e333dab`, redone off the release branch because it is a bug fix and the buggy code is byte-identical on 26.1 and dev. Diagnosed as distinct from [#22840](https://github.com/galaxyproject/galaxy/issues/22840) despite both living in `DatasetsService.report` — #21198 raises at the `resolve_job_markdown` call and never reaches the tuple unpack #22840 fixes on the next line, and that unpack is a dev-only regression since 26.1 still has the 2-tuple form, so #23599 needs no backport. `resolve_job_markdown` had `raise Exception("Unknown exception")` for outputs and an unguarded `inp_data[name]` subscript for inputs, both from `1f95f421c92`; both now route through one `_resolve_job_reference` raising `MalformedContents`, following `WorkflowInvocation.get_output_object`'s precedent. Reproduced without a custom tool by uploading a `tool_markdown` dataset with a bad label, since the endpoint resolves against the creating job. Two API tests, verified red first at 500 on 26.1 then green; the three `test_markdown_export.py` unit tests written in the first pass were dropped at John's request as the API tests read better; `test/unit/app/managers/` 395 passed; ruff/black/isort/flake8/mypy/pre-commit clean; [the description used](issue_21198_markdown_label_errors_pr_description.md).
- [#23605](https://github.com/galaxyproject/galaxy/pull/23605) — branch `test_case_validation_model_parse_errors` — Description: Replays #23004 with tool-model parse reporting and case-insensitive Infinity bounds; blockers: monitor CI on `36d71aa461f` and rebase onto dev once #23600 merges, since the PR currently carries that base's four commits as well as its own; opened as a draft on 2026-09-20 against dev from the prepared description with the dependency block cut from two paragraphs to two sentences at John's request — #23600 is now an open PR so it is referenced by number instead of by fork branch URL — and the richard-burhans credit written without the at-sign so opening it pinged nobody; the tip was amended to `36d71aa461f` to drop the five-line rationale comment above the `try:` in `validate_test_cases_for_tool_source`, which duplicated the commit message body, and to add the missing `Co-Authored-By` trailer; 26 test-case, 104 linter, and 18 upgrade-advisor unit tests pass against a copy-on-write venv clone, with `ruff check` and `ruff format --check` clean.
- [#23606](https://github.com/galaxyproject/galaxy/pull/23606) — branch `issue_22705_job_icj_id` — Description: Exposes `implicit_collection_jobs_id` on `GET /api/jobs/{id}` and `POST /api/jobs/search`, dropping an ICJ trawl; blockers: monitor CI on `a672d44b8eb` — opened as a draft by John on 2026-09-20 against dev, mergeable, with all 45 checks still pending at open; fixes [#22705](https://github.com/galaxyproject/galaxy/issues/22705), squashed to one atomic commit on dev `48c7d7028fc` the same day and force-pushed, with the diff verified byte-identical to the three-commit tree apart from the dropped `test_model.py` scoping guard — so the element-view-only scoping now rests on the commit message rather than on a test, since no API-level test can pin it while index rows resolve to `JobSummary`; subagent review applied before the squash — `/api/jobs/search` covered and agent id encoding fixed; the three field tests and the agent-encoding assertion were each verified red first, full `test_workflow_extraction.py` 65 passed 1 skipped, regenerated client schema with `pnpm type-check` clean; [the description used](issue_22705_job_icj_id_pr_description.md).
- [#23607](https://github.com/galaxyproject/galaxy/pull/23607) — branch `playwright_unlock_workflow_run` — Description: Drops 24 `selenium_only` decorators from `test_workflow_run.py` and ports `_chipseq_data_entry` off `action_chains`; blockers: monitor CI on `7da479e62f4` and undraft once settled; John opened this draft himself on 2026-09-20 against dev, squashed to one commit of 48 insertions and 72 deletions in a single file — the head moved from the `2b462ddbe38` this file recorded — and titled it `Unlock test_workflow_run for Playwright`; 28 of 34 tests pass under Playwright locally and the remaining six are environment-blocked here rather than backend gaps, two needing `enable_celery_tasks`, two failing identically under Selenium, and the chipseq `from_list_pairs` variant passing under Selenium while its Playwright failures land on a different hid each run at load average 20–39, with the `from_uris` chipseq test passing under both backends to verify the port. Fork CI on `7da479e62f4` has settled at 11 green of 12, the one red being Integration Selenium's `test_history_import_export_ftp.py::test_history_export_tracking` timing out on `.go-next-btn` - a different directory from the single file this branch touches, and the same content passed that job at `2b462ddbe38`.
- [#23610](https://github.com/galaxyproject/galaxy/pull/23610) — branch `config_rebuild_drift_check` — Description: Rebuilds the generated config artifacts on dev and adds CI so they cannot go stale again; blockers: monitor CI on `6d28c74bea6` and undraft once settled — John opened this draft himself on 2026-09-20 against dev, mergeable, with 42 of 49 checks still in progress at open and none red. **The new job already ran and passed on real CI** — `Check generated config files are up to date (3.10)`, [run 35532273971](https://github.com/galaxyproject/galaxy/actions/runs/35532273971/job/106134867147) — so the workflow is verified in GitHub Actions, not just simulated locally. Four commits on dev `7524f5e2507`. The drift: the generated type stub was missing `expression_evaluation_isolation_command`, and two option descriptions were wrapped differently than the generator emits. The reason it recurs: the generators emitted a trailing blank line at EOF and turned blank description lines into `"# "`, both of which `end-of-file-fixer` and `trailing-whitespace` in `.pre-commit-config.yaml.sample` strip back out — `git log` on `galaxy.yml.sample` shows the EOF byte alternating across the last ~25 commits that touched it. Without that fix the check would be a trap: regenerate, commit with pre-commit installed, get told by CI to regenerate. `.github/workflows/config_drift.yaml` copies the `lint_openapi_schema.yml` shape but uses a `paths:` allowlist — `lib/galaxy/config/**` (schemas, generators, four of the five outputs), `pinned-requirements.txt` (the gravity pin feeds `settings_to_sample()`), `Makefile`, `doc/source/admin/galaxy_options.rst` (the one output outside the config package) and the workflow itself — plus the `cff-validator` push guard and one Python version, not a matrix. That is ~3% of recent dev commits against ~100% for a `paths-ignore` list; both commits that produced this drift (`7f2e730c6c9`, `2919a996363`) match the filter. The list is duplicated across `push` and `pull_request` because GitHub Actions does not expand YAML anchors — zizmor flags them. Verified: generators idempotent by md5 across two runs, 73 `test/unit/config/` tests pass, workflow clean under `check-jsonschema` and `zizmor`, and the three content commits were made with the local pre-commit hooks enabled and passed all of them — the opposite of the `--no-verify` the #22437 branch needed. Open question carried into the PR: `config_manage.py` also imports `galaxy.util.yaml_util`/`properties`/`resources`, which sit outside the filter, so an emit-ordering change there would surface on the next config-touching PR rather than its own. [The description prepared](config_rebuild_drift_check_pr_description.md) — John opened with his own shorter body instead.

## Draft Galaxy PRs — needs author work (`author_work`)

- [#23455](https://github.com/galaxyproject/galaxy/pull/23455) — branch `pick_value_first_ok_or_skip` — Description: Adds a pick-value mode that ignores failed or null inputs and skips without usable values; blockers: depends on #23433 and has three completed red CI checks.
- [#23330](https://github.com/galaxyproject/galaxy/pull/23330) — branch `pja_guard_mapped_over_skipped` — Description: Prevents post-job actions from applying to skipped mapped-over workflow outputs; blockers: approved but depends on #23433 and has one completed red CI check.
- [#23326](https://github.com/galaxyproject/galaxy/pull/23326) — branch `htcondor_pulsar` — Description: Shares HTCondor mechanics with Pulsar and bounds held-job handling with a grace window; blockers: Pulsar #486 needs rebase and resolution of its hold/retry-policy thread.
- [#22996](https://github.com/galaxyproject/galaxy/pull/22996) — branch `wf_tool_state` — Description: Adds gxwf CLI tooling for static validation and conversion of stateful Galaxy workflows; blockers: resolve merge conflicts and triage two completed red CI checks.
- [#22988](https://github.com/galaxyproject/galaxy/pull/22988) — branch `notebook_iframe` — Description: Explores embedded Galaxy notebooks in Loom with automatic synchronization; blockers: resolve merge conflicts and triage failing CircleCI.
- [#21806](https://github.com/galaxyproject/galaxy/pull/21806) — branch `fix_copied_datasets` — Description: Fixes workflow extraction when datasets were copied across histories; blockers: resolve merge conflicts and triage eight completed red CI checks.
- [#21199](https://github.com/galaxyproject/galaxy/pull/21199) — branch `test-stories` — Description: Adds executable tutorial stories for Galaxy developers and automated documentation; blockers: resolve merge conflicts and triage two completed red package checks.
- [#19937](https://github.com/galaxyproject/galaxy/pull/19937) — branch `shed_redirect` — Description: Redirects a legacy Tool Shed URL pattern for Galaxy 26.0 compatibility; blockers: monitor the fresh CI on `3a0c2128cc2` and triage whatever survives of the nine reds that were on `3c7c35a1ce`; the rebase was force-pushed on 2026-09-18, replaying five commits without conflict onto the `release_26.0` tip `f522e3ea575` and adding 76 lines across four files, with 40 checks queued and the February-generated Tool Shed `schema.ts` chunk still unverified against a fresh regeneration.
- [#15868](https://github.com/galaxyproject/galaxy/pull/15868) — branch `typed_exceptions` — Description: Strengthens typing around Galaxy exception classes and their consumers; blockers: approved but needs merge-conflict resolution and current CI.
- [#12238](https://github.com/galaxyproject/galaxy/pull/12238) — branch `collection_restructure_panel` — Description: Adds a panel for transforming dataset collection layouts; blockers: needs merge-conflict resolution and current CI.
- [#9911](https://github.com/galaxyproject/galaxy/pull/9911) — branch `pulsar-mq-pull` — Description: Polls for Pulsar MQ jobs potentially missed by normal event handling; blockers: needs merge-conflict resolution, current CI, and resolution of its old review thread.
- [#8846](https://github.com/galaxyproject/galaxy/pull/8846) — branch `job_files_refactor` — Description: Refactors the job-files API toward a microservice-compatible boundary; blockers: needs merge-conflict resolution and current CI.

## Galaxy branches — needs a PR (`branches_need_pr`)

- Branch `issue_22437_account_interface_enforcement` — Description: Enforces `enable_account_interface` server-side for [#22437](https://github.com/galaxyproject/galaxy/issues/22437) — the flag gated the UI and the read side of the account API only, so any authenticated user could change their email, username or password, or delete their account, via the API and desynchronize from an SSO/OIDC/LDAP directory; blockers: open against `dev` using [the prepared description](issue_22437_account_interface_enforcement_pr_description.md); one commit `358ea3d2472` on dev `7524f5e2507`, pushed to `jmchilton`. Guards sit in the API controllers, not `UserManager`, because `psa_authnz.sync_user_profile` writes the same fields through the manager precisely when the option is off; all four raise `ConfigDoesNotAllowException` (403) following the `allow_local_account_creation` precedent in `create` on the same controller. `PUT /api/users/{user_id}` is guarded per-field so `preferred_object_store_id` stays writable (John's call over the gist's block-the-whole-endpoint proposal), and token password resets go through `controllers/user.py` so account recovery is unaffected. All four rejections verified red first as real 200s — including `Password has been changed.` — and the assertions pin `err_code` 403004 because `AuthenticationRequired` is also a 403 and gave a false green until the test used the correct current password; 9 passed in `test_config_options_users.py`, 25 in `test_users.py` API, 13 across three user integration modules, lint clean. Committed with `--no-verify`: the local hook rewrites untouched whitespace across the whole `galaxy.yml.sample` header and CI runs neither pre-commit nor a config-rebuild drift check, so that churn is deliberately kept out of the diff.
- Branch `playwright_collection_builders` — Description: Drops all 8 `selenium_only` decorators from `test_collection_builders.py` so the file runs under Playwright; blockers: open against `dev` using [the prepared description](playwright_collection_builders_pr_description.md); pushed at `5b93f94c5ad` on dev `48c7d7028fc`, no source change was needed and the full file gives 8 passed with no failures or skips, helped by the file's use of `@managed_history` which gives every test its own history.
- Branch `playwright_history_panel_collections` — Description: Drops all 9 remaining `selenium_only` decorators from `test_history_panel_collections.py` so the whole file runs under Playwright; blockers: open against `dev` using [the prepared description](playwright_history_panel_collections_pr_description.md); pushed at `e2640d8e00e` on dev `48c7d7028fc`, no source change was needed and the full file gives 9 passed and 3 failed under Playwright, where all 3 were already running under Playwright before this branch, carry `@flakey`, fail identically under Selenium, and become skips in CI because `playwright.yaml` and `selenium.yaml` both set `GALAXY_TEST_SKIP_FLAKEY_TESTS_ON_ERROR=1`.
- Branch `playwright_hover_away` — Description: Adds `hover_away()` to the gesture vocabulary and makes Playwright's `action_chains()` raise instead of returning self; blockers: open against `dev` using [the prepared description](playwright_hover_away_pr_description.md); pushed at `512b7fb0e33` on dev `48c7d7028fc`, verified by 309 passing unit tests across the Selenium, Playwright and proxy driver fixtures plus a throwaway tooltip probe green under both backends — `_clear_tooltip` was the last unguarded `action_chains()` call in shared code, so every tooltip assertion was broken under Playwright and no CI job ran them there.
- Branch `metadata_lazy_imports` — Description: Combines Marius’s metadata lazy imports with Phase 0 while preserving older Python and current security checks; blockers: wait for fresh fork CI on `dba927b7ff8` and prepare a PR; all ten reds from the `9720f2e629a` run are addressed — the four Integration shards died at collection because dev renamed the netCDF4 fixture to `test_tas.netcdf4` in `225e4767cbf`, so the rebase onto dev `101e060a65c` unblocks collection without proving the suite, the two Unit reds were a package-boundary test that only passed when the shell exported PYTHONPATH, and the two Python linting plus two Test Galaxy packages reds were one `find("registration")` mypy error in `test_metadata.py` reached through the packages test symlink; locally 29 metadata and 9 discovery unit tests pass, the discovery fix is verified red-to-green with PYTHONPATH unset, and the datatype-upload integration module collects 335 tests again.
- Branch `toolshed_delegate_publishing` — Description: Allows push collaborators to update Tool Shed metadata and shows the repository owner in push access; blockers: triage nine completed red fork workflows on `37d06ffc1e4`, then prepare the description and open a PR; no CI remains pending, rebased onto dev `07f42d2a5a5` with an explicit permission-change commit message and 13 focused API tests passing again, with [Tool Shed CI tracked here](https://github.com/jmchilton/galaxy/actions/runs/35134282289).
- Branch `when_expression_analysis` — Description: Matches workflow `when` inputs structurally instead of by substring; blockers: triage four completed fork CI failures, then open using `OPT_INPUT_PR_2_DESCR.md`.
- Branch `issue_23424_when_expression_validation` — Description: Rejects obsolete pipe-delimited tool-input paths during workflow import; blockers: triage Mulled, Playwright, Selenium, and package failures on `2938c2448c6`, then open using `WHEN_VALIDATION_PR_DESCR.md`.
- Branch `optional_input_gating` — Description: Adds authoring and validation for running steps only when optional inputs are present; blockers: triage five completed fork CI failures, then open using `OPT_INPUT_PR_3_DESCR.md`.
- Branch `workflow_input_pipe_names` — Description: Reserves pipes in input names, with validation-only, top-level correction, and nested correction alternatives; blockers: fix name-mangled private-method mypy calls and import ordering in test_workflow_input_names for all three variants, then choose a scope from [the comparison note](workflow_input_pipe_names_versions.md); all fork CI has settled, validation-only and top-level have only Python linting red, nested also failed macOS uv installation, and no variant rewrites expressions.
- Branch `resubmit_static_chain_test_26.1` — Description: Adds integration coverage for a job resubmitting more than once through a chain of static destinations with a per-hop `<env>`; test-only, no behavior change; blockers: open against `release_26.1` using [the prepared description](resubmit_static_chain_test_pr_description.md), and decide whether to keep the `Co-Authored-By: Anthony Bretaudeau` trailer and whether `dev` is the better target for a test-only change. One commit `40cdf413f74` on release_26.1 `4e33e333dab`, all pre-commit hooks passed. **Fork-only — no local branch or worktree**: pruned from disk on 2026-09-20, so recover it with `git fetch jmchilton resubmit_static_chain_test_26.1` before working on it. Ported from #12852 after it was closed as superseded — both bugs it reported are fixed (#19753 restored env and tags on resubmission, #22670 deferred dynamic destination evaluation adopting #9747's approach) but its test never merged, and the static-chain path stayed uncovered: dev has only a single static hop plus #22670's chained *dynamic* test. Verified red-to-green on a real run rather than by reasoning — `1 passed in 39.69s` as written, and a negative control removing the second `<resubmit>` hop gives `1 failed — assert False`, so the assertion is not vacuous despite `_assert_job_fails` actually asserting the job succeeded (the tool test is `expect_failure="true"`, so `_run_tool_test` raises on success).
- Branch `cwl_fixes_5` — Description: Collects CWL validation, typing, output-path, and job-document compatibility fixes; blockers: rebase, replay subworkflow mapping, run conformance tests, and decide the PR split.
- Branch `subworkflow_mapping` — Description: Implements ordered-axis mapping and delayed pass-through for callable subworkflows; blockers: preserve four modified files, rebase 14 commits onto `dev`, validate, and open a PR.
- Branch `subworkflow_mapping_when_alignment` — Description: Aligns mapped subworkflow when-values with compatible collection inputs; blockers: determine its stack relationship, rebase, validate, and open a PR.

## Galaxy branches — still deciding on (`branches_need_decision`)

- Branch `pulsar_remote_job_directory` — Description: Uses Pulsar-reported remote job/tool directories and documents remote-directory tokens in job-config samples; blockers: open against `dev` using [the prepared description](pulsar_remote_job_directory_pr_description.md), CI on `c7f98f8f4b6` is settled and greenish with 18 successful workflows and three unrelated reds from expression-worker tests and an Apptainer download 503; companion to [Pulsar #512](https://github.com/galaxyproject/pulsar/pull/512), independent of it in either order, with 13 focused unit tests passing locally red-to-green and the docs commit droppable if it should wait for #512.


## Open PRs in other projects (`other_prs`)

- [Pulsar #486](https://github.com/galaxyproject/pulsar/pull/486) — branch `htcondor2` — Description: Adds a queued HTCondor manager using shared Galaxy/Pulsar mechanics; blockers: rebase and resolve the hold/retry-policy review thread before Galaxy #23326.

- [IWC #1366](https://github.com/galaxyproject/iwc/pull/1366) — branch `codex/expand-claude-review-command` — Description: Expands the agent review command for IWC workflows; blockers: required review, CI has no failing checks.
- [IWC #1365](https://github.com/galaxyproject/iwc/pull/1365) — branch `deploy-report-only-on-failure` — Description: Reports deployment status only when deployment actually failed; blockers: required review, CI has no failing checks.
- [Planemo #1695](https://github.com/galaxyproject/planemo/pull/1695) — branch `issue-1693-iwc-missing-tests-error` — Description: Requires workflow tests under the IWC lint profile; blockers: required review, one CI job failed on Zenodo 504s and training-test fallout; #1698 addresses outage handling.
- [Planemo #1698](https://github.com/galaxyproject/planemo/pull/1698) — branch `skip-tests-when-zenodo-down` — Description: Skips Zenodo-dependent tests when its API is unavailable; blockers: required review, CI has no failing checks.
